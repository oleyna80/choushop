# .agent/skills/

Project-local skill definitions. Each skill is a directory with a `SKILL.md`.

Claude Code loads runtime skills from `.claude/skills/`. Bootstrap mirrors the
same skills here for project-neutral routing and other agent runtimes.

See `.agent/ROSTER.md` for the skill routing table.
