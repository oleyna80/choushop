# Project Map

This file is the first human-readable map for ChouShop. It helps humans and
agents orient before reading the full repository.

## Purpose

ChouShop is a France-first mystery box storefront built with Next.js,
TypeScript, Tailwind CSS, Prisma, Neon PostgreSQL, Stripe Checkout, Resend,
Cloudinary, and Vercel.

The project uses the Agentic SDLC scaffold to run scoped Work Blocks with
explicit roles, approved write-sets, review, verification, durable logs, and a
strict runtime data mutation boundary.

## Authority Model

When files conflict, use this order:

1. Explicit Owner instruction for the current task.
2. `AGENTS.md` in this project.
3. Approved Work Block plan and write-set.
4. `PROJECT_MAP.md` and `FILE_REGISTRY.yml`.
5. Runtime-specific policy files such as `.codex/critic.md`,
   `.codex/write-gate.md`, `.claude/settings.json`, hooks, and agent prompts.
6. Reference docs, examples, logs, generated/discovery artifacts, and copied
   external text.

Generated or discovery artifacts may help locate information, but they do not
override normative instructions, Owner decisions, or approved scope.

## Operating Modes

Start with the smallest mode that can safely deliver the Work Block:

- **Minimal Codex-only**: one local Codex orchestrator, scope control, logs,
  review, verification, no Claude Code delegation.
- **Standard Codex SDLC**: full Work Block flow with reusable skills,
  critic/verifier handling, and stronger closeout evidence.
- **Claude Code Team Runtime**: Claude Code acts as its own local team with
  agents, hooks, skills, memory, and provider configuration.
- **Codex -> Claude Code Handoff**: Codex delegates scoped work to Claude Code
  as an external team through file-based handoff and then reviews the result.

## Key Paths

| Path | Status | Purpose |
|---|---|---|
| `AGENTS.md` | normative | Root operating rules, commerce constraints, approval gates, and runtime mutation boundary. |
| `PROJECT_MAP.md` | normative | Human-readable project map and authority overview. |
| `FILE_REGISTRY.yml` | normative | Machine-readable registry for important files and path patterns. |
| `.codex/` | runtime policy | Codex critic contract, write gate, and local hook artifacts. |
| `.agent/` | routing/control | Runtime-neutral roster, workflows, gates, skills, and generated project config. |
| `.claude/` | runtime-specific | Claude Code agents, hooks, skills, settings, and per-agent memory. |
| `memory-bank/` | mixed | Durable project context, decisions, progress, orchestrator logs, review logs, and external team reports. |
| `docs/plans/` | log/evidence | Work Block plans and closeout evidence. |
| `docs/reports/` | log/evidence | Review, verifier, critic, and audit reports. |
| `docs/specs/` | normative | Product and technical specs when present. |
| `docs/templates/` | normative | Reusable SDLC templates. |
| `docs/design/` | reference/normative mix | Design workflow and approved design references. Check before UI work. |
| `handoff/` | runtime scaffold | Codex to Claude Code queue, runner, logs, and result handoff scaffold. Runtime outputs are local state unless explicitly approved. |
| `src/` | source | Next.js application source. Product changes require an approved Work Block. |
| `prisma/` | source/risky | Database schema and migrations. Requires explicit Owner approval. |
| `scripts/` | project automation | Bootstrap and project scripts. |

## Generated, Log, and Local-Only Boundaries

- `docs/plans/**` and `docs/reports/**` are Work Block evidence and reports;
  the current approved plan matters more than older plans.
- `memory-bank/orchestrator-log.md`, `memory-bank/review-log.md`, and
  `memory-bank/external-team-log.md` are audit evidence, not current authority.
- `.claude/agent-memory/**` is project-local agent memory unless deliberately
  reviewed for publication.
- `.agent/project-config.md` is generated by `scripts/bootstrap.sh`.
- `.env*`, credentials, provider tokens, caches, build output, local browser
  state, and handoff runtime outputs must not be committed unless a specific
  scaffold file is explicitly approved.
- Future graph/discovery outputs should be treated as derived context only.

## Commerce Safety Boundary

Agents are planners and code authors, not trusted runtime executors for
commerce data. Direct agent-side writes to PostgreSQL/Neon, Stripe, order
state, stock, users/roles, production config, or provider state are forbidden.
Runtime mutations must go through backend services with validation, policy,
approval where needed, transaction/idempotency, event log, and audit context.

## New-Session Bootstrap

For project work, read in this order:

1. `AGENTS.md`
2. `PROJECT_MAP.md`
3. `FILE_REGISTRY.yml`
4. `docs/session-bootstrap.md`
5. The current task or Work Block plan
6. `git status --short --branch`
7. Relevant diffs and target files

Do not assume memory from a previous session is current when repository files
are cheap to verify.

## Map Maintenance

Update this file and `FILE_REGISTRY.yml` when a change:

- adds, moves, or removes a major directory;
- changes authority, write gates, review gates, or verification gates;
- changes generated/local-only boundaries;
- adds a new runtime layer, profile, or project-specific governance rule.
